{
  "meta": {
    "title": "Dock base",
    "description": "Shared base every component depends on (endpoints, cross-slot state, config-page skeleton) — runs with the kit; keep it on, disabling it stops the other components from loading."
  }
}
